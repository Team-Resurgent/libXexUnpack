name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  native_builds:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            build_target: win-x64
            artifact_name: build\Release\libXexUnpack.dll
          - os: ubuntu-latest
            build_target: linux-x64
            artifact_name: build/Release/libXexUnpack.so
          - os: macos-latest
            build_target: osx
            artifact_name: build/libXexUnpack.dylib
    name: ${{ matrix.build_target }} Native Build

    steps:
      - uses: actions/checkout@v3

      - name: Configure
        run: cmake -B build
    
      - name: Build
        run: cmake --build build --config Release
        
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: latest
          draft: false
          files: ${{ matrix.artifact_name }}
        
      - name: Upload ${{ matrix.build_target }} Native Library
        uses: actions/upload-artifact@v2
        with:
          name: ${{ matrix.build_target }}
          path: ${{ matrix.artifact_name }}
          if-no-files-found: error
        
